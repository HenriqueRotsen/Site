# Copy to .env.local for local development (do not commit secrets)
REACT_APP_CONTACT_API_URL=http://127.0.0.1:8787
# Deixe vazio para usar o proxy do React (recomendado no dev). Em produção o CI define a URL completa.
# Copy to .env.local for local development (do not commit secrets)
REACT_APP_CONTACT_API_URL=http://127.0.0.1:8787
# Deixe vazio (ou omita) no dev para usar o proxy do React em /admin /auth /client
REACT_APP_BILLING_API_URL=
